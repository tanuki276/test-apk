name: APK Java ビルド (最終安定化)

on:
  release:
    types: [created]
  push:
    branches: [ main, master ] 
  workflow_dispatch: 

jobs:
  build:
    # 環境安定化のため ubuntu-22.04 に固定
    runs-on: ubuntu-22.04 

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      # 2. Java 17 のセットアップ (actions/setup-java@v4 は信頼できるため維持)
      - name: 2. Java 17 のセットアップ
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: 3. Android SDKパスの設定とツールのダウンロード
        run: |
          # SDKルートとパス変数の設定
          echo "ANDROID_SDK_ROOT=$HOME/android-sdk" >> $GITHUB_ENV
          
          # コマンドラインツールのダウンロードと展開
          mkdir -p $HOME/android-sdk
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O android-clt.zip
          unzip -q android-clt.zip -d $HOME/android-sdk/cmdline-tools
          mv $HOME/android-sdk/cmdline-tools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest
          rm android-clt.zip
          
          # sdkmanagerのパスをPATHに登録（次のステップで有効になる）
          echo "$HOME/android-sdk/cmdline-tools/latest/bin" >> $GITHUB_PATH

      # 🚨 修正: 独立したステップで SDK コンポーネントをインストール
      - name: 4. SDKコンポーネントのインストールとライセンス承認
        run: |
          # PATHが読み込まれた状態で sdkmanager を実行
          yes | sdkmanager --licenses
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: 5. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      # 6. Gradleシステムキャッシュを強制削除 (安定化のため維持)
      - name: 6. Gradleシステムキャッシュを強制削除
        run: rm -rf ~/.gradle/caches

      - name: 7. リリースAPKのビルド (assembleRelease)
        run: ./gradlew clean assembleRelease --no-daemon -Dgradle.parallel=false --stacktrace --info

      - name: 8. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          path: app/build/outputs/apk/release/
          retention-days: 7