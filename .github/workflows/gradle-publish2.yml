name: APK Java ビルド (最終安定化)

on:
  release:
    types: [created]
  push:
    branches: [ main, master ] 
  workflow_dispatch: 

jobs:
  build:
    # 環境安定化のため ubuntu-22.04 に固定
    runs-on: ubuntu-22.04 

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      # 2. Java 17 のセットアップ
      - name: 2. Java 17 のセットアップ
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      # 3. Android SDK セットアップ (v3対応 - インデント修正済み)
      - name: 3. Android SDK セットアップ (v3対応)
        uses: android-actions/setup-android@v3 
        with:
          # v3 で有効なパラメーターを使用
          android-sdk-version: '34' 
          # 🚨 修正箇所: components リストを with: のインデントから2スペース深くしました
          components: |
            platform-tools
            platforms;android-34
            build-tools;34.0.0
          
      - name: 4. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      - name: 5. Gradleシステムキャッシュを強制削除
        run: rm -rf ~/.gradle/caches

      - name: 6. リリースAPKのビルド (assembleRelease)
        run: ./gradlew clean assembleRelease --no-daemon -Dgradle.parallel=false --stacktrace --info

      - name: 7. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          path: app/build/outputs/apk/release/
          retention-days: 7