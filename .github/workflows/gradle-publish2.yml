name: APK Java ビルド (リリース)

on:
  release:
    types: [created]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 2. JDK 17のセットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 3. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      # ----------------------------------------------------
      # 🚨 強制リセットステップ 🚨 (デーモン停止とキャッシュ削除の組み合わせ)
      # ----------------------------------------------------
      - name: 3.5. Gradleシステムキャッシュを強制削除
        # 前回と同じ。システムキャッシュを確実にクリアします。
        run: rm -rf ~/.gradle/caches

      # ----------------------------------------------------
      # ビルドタスクの実行 (★修正点: --no-daemon を追加)
      # ----------------------------------------------------
      - name: 4. リリースAPKのビルド (assembleRelease)
        # 🟢 --no-daemon を追加し、Gradleデーモンを使用せず、単一プロセスでビルドを強制します。
        # これにより、デーモンが保持している可能性のある破損したセッション情報を排除します。
        run: ./gradlew clean assembleRelease --no-daemon --stacktrace --info

      # ----------------------------------------------------
      # 成果物のアップロード
      # ----------------------------------------------------
      - name: 5. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          path: app/build/outputs/apk/release/
          retention-days: 7