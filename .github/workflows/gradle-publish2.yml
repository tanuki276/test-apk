name: APK Java ビルド (最終安定化)

on:
  release:
    types: [created]
  push:
    branches: [ main, master ] 
  workflow_dispatch: 

jobs:
  build:
    # 環境安定化のため ubuntu-22.04 に固定
    runs-on: ubuntu-22.04 

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      # 🚨 修正ステップ 1: Java 17 のセットアップを公式アクションで分離
      - name: 2. Java 17 のセットアップ
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle' # Gradleキャッシュを有効化

      # 🚨 修正ステップ 2: Android SDKのセットアップをv4の正しいパラメーターで実行
      - name: 3. Android SDK セットアップ (v4対応)
        uses: android-actions/setup-android@v3
        with:
          # v4では古い 'android-sdk-version' の代わりに 'packages' を使用します。
          packages: |
            'platform-tools'
            'platforms;android-34' # compileSdk 34に必要なパッケージ
            'build-tools;34.0.0'

      - name: 4. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      - name: 5. Gradleシステムキャッシュを強制削除
        run: rm -rf ~/.gradle/caches

      - name: 6. リリースAPKのビルド (assembleRelease)
        run: ./gradlew clean assembleRelease --no-daemon -Dgradle.parallel=false --stacktrace --info

      - name: 7. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          path: app/build/outputs/apk/release/
          retention-days: 7