name: APK Java ビルド (リリース)

on:
  # メイントリガー: GitHubリポジトリで新しいリリースが作成されたときに実行
  release:
    types: [created]

  # ★手動実行を許可: Actionsタブからいつでも実行可能
  workflow_dispatch: 

jobs:
  build:
    # 標準的なLinux環境で実行
    runs-on: ubuntu-latest

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 2. JDK 17のセットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: 3. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      # ----------------------------------------------------
      # ビルドタスクの実行（コンフリクト解消後の確定版）
      # ----------------------------------------------------
      - name: 4. リリースAPKのビルド (assembleRelease)
        # エンドユーザー向けの配布物（APK）を生成するタスクを実行
        run: ./gradlew assembleRelease --stacktrace --info

      # ----------------------------------------------------
      # 成果物のアップロード（コンフリクト解消後の確定版）
      # ----------------------------------------------------
      - name: 5. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          # 🟢 APKの格納場所
          path: app/build/outputs/apk/release/
          # 成果物を保持する期間
          retention-days: 7
