name: APK Java ビルド (リリース)

on:
  release:
    types: [created]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 2. JDK 17のセットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          # キャッシュの自動復元は停止
          # cache: 'gradle' は意図的に削除しています

      - name: 3. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      # ----------------------------------------------------
      # 🚨 強制リセットステップ 🚨
      # ----------------------------------------------------
      - name: 3.5. Gradleシステムキャッシュを強制削除 (最後の手段)
        # 頑固なAGPのパス解決エラーを排除するため、ランナーの全キャッシュを強制削除します。
        run: rm -rf ~/.gradle/caches

      # ----------------------------------------------------
      # ビルドタスクの実行
      # ----------------------------------------------------
      - name: 4. リリースAPKのビルド (assembleRelease)
        # clean も実行して、ビルドディレクトリも確実にクリアします。
        run: ./gradlew clean assembleRelease --stacktrace --info

      # ----------------------------------------------------
      # 成果物のアップロード
      # ----------------------------------------------------
      - name: 5. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          path: app/build/outputs/apk/release/
          retention-days: 7