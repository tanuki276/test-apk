name: APK Java ビルド (リリース)

on:
  # メイントリガー: GitHubリポジトリで新しいリリースが作成されたときに実行
  release:
    types: [created]

  # 🟢 追加: メインブランチへのコード変更時に自動実行ううう
  push:
    branches: [ main, master ] 

  # 手動実行を許可
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 2. JDK 17のセットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          # キャッシュ復元は無効 (以前の修正)

      - name: 3. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      # 🚨 強制リセット: システムキャッシュを完全に破壊
      - name: 3.5. Gradleシステムキャッシュを強制削除
        run: rm -rf ~/.gradle/caches

      # ----------------------------------------------------
      # 🚨 最終回避策: ビルドタスクの実行 (競合防止)
      # ----------------------------------------------------
      - name: 4. リリースAPKのビルド (assembleRelease)
        # 🟢 --no-daemon: デーモンによる破損セッション情報の保持を無効化
        # 🟢 -Dgradle.parallel=false: タスクの並列実行を無効化し、リソース競合を強制的に防ぐ
        run: ./gradlew clean assembleRelease --no-daemon -Dgradle.parallel=false --stacktrace --info

      # ----------------------------------------------------
      # 成果物のアップロード
      # ----------------------------------------------------
      - name: 5. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          # APKの格納場所
          path: app/build/outputs/apk/release/
          retention-days: 7