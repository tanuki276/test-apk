name: APK Java ビルド (最終安定化)

on:
  release:
    types: [created]
  push:
    branches: [ main, master ] 
  workflow_dispatch: 

jobs:
  build:
    # 環境安定化のため ubuntu-22.04 に固定
    runs-on: ubuntu-22.04 

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      # 2. Java 17 のセットアップ (actions/setup-java@v4 は維持)
      - name: 2. Java 17 のセットアップ
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      # 🚨 最終修正: Android SDK セットアップアクションを @v4 に確定し、パラメーターを修正
      - name: 3. Android SDK セットアップ (v4確定)
        uses: android-actions/setup-android@v4 # 👈 @v4 を使用
        with:
          # v4 で必要なパラメーターを使用
          cmdline-tools-version: 'latest' # 最新のコマンドラインツールを使用
          packages: |
            platform-tools
            platforms;android-34
            build-tools;34.0.0
          
      - name: 4. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      - name: 5. Gradleシステムキャッシュを強制削除
        run: rm -rf ~/.gradle/caches

      - name: 6. リリースAPKのビルド (assembleRelease)
        run: ./gradlew clean assembleRelease --no-daemon -Dgradle.parallel=false --stacktrace --info

      - name: 7. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          path: app/build/outputs/apk/release/
          retention-days: 7