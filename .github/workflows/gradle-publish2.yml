name: APK Java ビルド (リリース)

on:
  release:
    types: [created]

  # メインブランチへのコード変更時に自動実行
  push:
    branches: [ main, master ] 

  # 手動実行を許可
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest

    # ----------------------------------------------------
    # 🚨 最終修正: 環境変数の確実なセットアップ 🚨
    # run:ブロックの外側で env: を使用し、子プロセスに確実に渡す
    # ----------------------------------------------------
    env:
      ANDROID_HOME: ${{ github.workspace }}/.android-sdk
      # Android SDK toolsのパスをPATHに追加
      PATH: ${{ github.workspace }}/.android-sdk/cmdline-tools/latest/bin:${{ github.workspace }}/.android-sdk/platform-tools:${{ github.workspace }}/.android-sdk/tools:${{ env.PATH }}

    steps:
      - name: 1. リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 2. JDK 17のセットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          # Android SDKの自動セットアップは無効（環境変数で手動設定するため）
          
      - name: 3. gradlew に実行権限を付与
        run: chmod +x ./gradlew

      # ----------------------------------------------------
      # 🚨 強制リセットステップ 🚨
      # ----------------------------------------------------
      - name: 3.5. Gradleシステムキャッシュを強制削除
        run: rm -rf ~/.gradle/caches

      # ----------------------------------------------------
      # 4. リリースAPKのビルド (assembleRelease)
      # ----------------------------------------------------
      - name: 4. リリースAPKのビルド (assembleRelease)
        # 🟢 --no-daemon: デーモンによる破損セッション情報の保持を無効化
        # 🟢 -Dgradle.parallel=false: タスクの並列実行を無効化
        # 🟢 --stacktrace: ログを詳細化し、エラーの根本原因を特定
        run: ./gradlew clean assembleRelease --no-daemon -Dgradle.parallel=false --stacktrace

      # ----------------------------------------------------
      # 成果物のアップロード
      # ----------------------------------------------------
      - name: 5. リリースAPKの成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: liefantidia-apk
          path: app/build/outputs/apk/release/
          retention-days: 7