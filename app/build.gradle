// Top-level plugins definition
plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.example.liefantidia2'
    compileSdk 34

    defaultConfig {
        // アプリケーションIDはプロジェクト全体で一貫させる
        applicationId "com.example.liefantidia2" 
        minSdk 26 // KeyStore/Biometricの安定性と互換性を考慮し26を維持
        targetSdk 34
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    // 💡 Debugビルドを使用するため、signingConfigsブロック全体を削除

    buildTypes {
        release {
            // Minify/難読化設定
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'

            // 💡 Debugビルドではデフォルトの署名が使われるため、ここでの署名設定は不要
        }
    }

    // Java/Kotlinのターゲットバージョンは11を維持し、広く使われている環境に合わせます
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = '11'
    }
}

dependencies {
    // AndroidX Core and UI libraries
    implementation 'androidx.core:core-ktx:1.13.1' 
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0' 
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    // BiometricPrompt - 生体認証と安全なキー管理に使用
    // バージョンは最新安定版付近の 1.2.0-alpha05 を維持
    implementation 'androidx.biometric:biometric:1.2.0-alpha05' 

    // OkHttp - Gemini APIへの通信に使用
    implementation 'com.squareup.okhttp3:okhttp:4.12.0' 

    // Test dependencies
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}